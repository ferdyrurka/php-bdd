default:
  suites:
    calculator:
      paths:
        - '%paths.base%/features/calculator'
      contexts:
        - Feature\Calculator\FeatureContext
    shop:
      paths:
        - '%paths.base%/features/shop'
      contexts:
        - Feature\Shop\Service\TaxFeatureContext
